import { UserDetailFormDirective } from './user-detail-form.directive';

describe('UserDetailFormDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDetailFormDirective();
    expect(directive).toBeTruthy();
  });
});
